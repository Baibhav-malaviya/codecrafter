export interface Vote {
	questionId: string;
	// Add other fields if necessary
}

export interface Answer {
	questionId: string;
	// Add other fields if necessary
}

export interface Question {
	$id: string;
	// Add other fields if necessary
}
